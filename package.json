{
  "name": "bloxfruits-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "vercel dev",
    "deploy": "vercel deploy",
    "build": "pip3 install -r templates/requirements.txt"
  },
  "dependencies": {
    "child_process": "^1.0.2"
  },
  "devDependencies": {
    "vercel": "^32.5.0"
  }
}
